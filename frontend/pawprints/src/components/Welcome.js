import React from 'react';
import './Welcome.css';

const Welcome = () => {
    return (
        <div>
            <h2>Welcome, Admin!</h2>
            <p>Use the navigation on the left to manage approvals.</p>
        </div>
    );
};

export default Welcome;
